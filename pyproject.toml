[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "nanocode"
version = "0.2.0"
description = "Minimal agentic coding CLI with streaming and auto model routing"
requires-python = ">=3.9"

[project.scripts]
nanocode = "nanocode.cli:main"
